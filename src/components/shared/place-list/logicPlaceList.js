import React, { Component, Fragment } from 'react';
import styles from './stylesPlaceList';

export default class PlaceInput extends Component {
  state = {
  };

  render () {
    return (
      <Fragment>
        {/* Content here */}
      </Fragment>
    );
  }
};
