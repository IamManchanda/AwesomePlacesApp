import React, { Component, Fragment } from 'react';
import styles from './styles';

export default class PrototypalComponent extends Component {
  state = {
  };

  render () {
    const {  } = this.props; // Props
    const {  } = this.state; // Data
    const {  } = this; // Methods
    const {  } = this; // Computed

    return (
      <Fragment>
        {/* Content here */}
      </Fragment>
    );
  }
};
